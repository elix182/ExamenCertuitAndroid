<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  package="com.certuit.pacheco.eliezer.examenclima">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
    tools:ignore="GoogleAppIndexingWarning">
    <activity android:name=".SearchActivity"
      android:label="@string/title_search_activity"
      android:parentActivityName=".WeatherActivity"/>
    <activity android:name=".FavoritesActivity"
      android:label="@string/title_favorites_activity"
      android:parentActivityName=".WeatherActivity"/>
    <activity
      android:name=".WeatherActivity"
      android:label="@string/title_weather_activity" />
    <activity
      android:name=".SplashscreenActivity"
      android:label="@string/title_splashscreen_activity"
      android:theme="@style/SplashTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

</manifest>